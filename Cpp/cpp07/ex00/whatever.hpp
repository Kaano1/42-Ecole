#ifndef _WHATEVER_HPP_
#define _WHATEVER_HPP_

using std::cout;
using std::endl;

template<typename T>
void swap(T& a, T& b);

template<typename T>
T min(T a, T b);

template<typename T>
T max(T a, T b);


#endif
